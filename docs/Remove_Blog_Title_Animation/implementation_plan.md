# 実装計画: ブログ記事タイトルのスクランブルアニメーション削除

## 1. レイアウトの更新 (`src/layouts/BlogPost.astro`)
- 対象要素: `h1` タグ（記事タイトル）
- 変更点:
  - `class="... scramble-text"` -> `scramble-text` クラスを削除。
  - `data-original-text={title}` -> 属性ごと削除。
- 期待される結果:
  - ページ読み込み時にタイトルが最初から完全に表示され、アニメーションによるちらつきやレイアウトシフトが発生しなくなる。
